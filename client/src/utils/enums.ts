export enum Difficulty {
  easy = 'easy',
  middle = 'middle',
  hard = 'hard'
}

export enum Locales {
  en = 'en',
  uk = 'uk'
}

export enum AlertType {
  success = "success",
  warning = "warning",
  error = "error"
}